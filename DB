import pyttsx3

text = '\ub124'
encoded_text = text.encode('ascii', errors='ignore')
conn = pymysql.connect(
    user="root",
    passwd="0000",
    host="127.0.0.1",
    db="interview_type",
    charset="UTF8"
)
cursor=conn.cursor()
